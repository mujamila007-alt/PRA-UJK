import { db, doc, getDoc } from './firebase.js';import { requireRole, bindLogout, escapeHtml } from './common.js';
const {user,profile}=await requireRole('peserta');bindLogout();document.getElementById('participant-name').textContent=profile.name||'-';
const [ss,as]=await Promise.all([getDoc(doc(db,'settings','exam')),getDoc(doc(db,'attempts',user.uid))]);const s=ss.exists()?ss.data():{title:'Ujian Digital Marketing',instructions:'Baca setiap soal dengan teliti.',durationMinutes:60,status:'nonaktif'};const a=as.exists()?as.data():null;document.getElementById('title').textContent=s.title||'Ujian Digital Marketing';document.getElementById('instructions').textContent=s.instructions||'';document.getElementById('duration').textContent=`${s.durationMinutes||60} menit`;const area=document.getElementById('action-area'),badge=document.getElementById('exam-badge');
if(a?.status==='selesai'){badge.textContent='UJIAN SELESAI';area.innerHTML=`<div class="participant-result-card" data-new-result="1"><div class="result-score-ring"><span>Nilai Anda</span><strong>${a.score??0}</strong><small>/ 100</small></div><div class="result-summary"><span class="result-complete-badge">✓ Ujian selesai</span><h2>Hasil ujian Anda</h2><p>Nilai dihitung otomatis dari jawaban yang sudah dikirim.</p><div class="result-mini-stats"><div><strong>${a.correctCount||0}</strong><span>Benar</span></div><div><strong>${a.wrongCount||0}</strong><span>Salah</span></div><div><strong>${a.totalQuestions||0}</strong><span>Total soal</span></div></div></div></div>`;}else if(s.status!=='aktif'){badge.textContent='UJIAN DITUTUP';area.innerHTML='<div class="alert warn"><span>Ujian belum dibuka oleh administrator.</span></div>';}else{badge.textContent='UJIAN AKTIF';const ongoing=a?.status==='berlangsung' && Number(a.deadlineMs)>Date.now();area.innerHTML=`<a class="btn btn-success" href="/ujian/mulai.html">${ongoing?'Lanjutkan Ujian':'Mulai Ujian Sekarang'} →</a>`;}
