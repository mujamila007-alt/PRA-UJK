const form=document.getElementById('setup-form'),box=document.getElementById('message'),btn=document.getElementById('submit');
form.addEventListener('submit',async e=>{e.preventDefault();box.hidden=true;btn.disabled=true;btn.textContent='Membuat...';try{const r=await fetch('/api/setup-admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:document.getElementById('name').value.trim(),email:document.getElementById('email').value.trim(),password:document.getElementById('password').value,secret:document.getElementById('secret').value})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Setup gagal.');box.className='alert ok';box.querySelector('span').textContent='Admin berhasil dibuat. Silakan login melalui Panel Admin.';box.hidden=false;form.reset();}catch(err){box.className='alert err';box.querySelector('span').textContent=err.message;box.hidden=false;}finally{btn.disabled=false;btn.textContent='Buat Administrator';}});
